# SeD 判别器改进进度跟踪

> 最后更新：2026-04-24

## 状态总览

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 1: 代码移植 | ✅ 完成 | module_attention.py + sed_discriminator.py |
| Phase 2: 训练器改造 | ✅ 完成 | base.py 多个方法修改 |
| Phase 3: 配置 | ✅ 完成 | sd2_train.yaml 新增配置项 |
| Phase 4: 验证（离线测试） | ✅ 完成 | 模块实例化 + Resize + backward 通过 |
| Phase 4: 验证（训练冒烟） | ⬜ 待执行 | 需 GPU 环境 |
| Phase 5: 消融实验 | ⬜ 待执行 | 基线 + 不同 λ 对比 |

---

## Phase 1: 代码移植 ✅

### T1.1 HYPIR/model/module_attention.py
- [x] 从 `basecode/SeD/models/module_attention.py` 移植
- [x] 移除未使用类（SpatialSelfAttention、LinearAttention）
- [x] 保持核心逻辑：CrossAttention、BasicTransformerBlock、ModifiedSpatialTransformer

### T1.2 HYPIR/model/sed_discriminator.py
- [x] 从 `basecode/SeD/models/sed.py` 移植 CLIP_Semantic_extractor + SeD_P
- [x] 修改 import 路径为 `from HYPIR.model.module_attention import ModifiedSpatialTransformer`
- [x] 移除 SeD_U（本次只用 SeD_P）

### T1.3 模块实例化测试
- [x] CLIP_Semantic_extractor: 输入 (1,3,256,256) → 输出 (1,1024,16,16) ✅
- [x] SeD_P: 输入 (1,3,256,256) + semantic (1,1024,16,16) → 输出 (1,1,30,30) ✅

---

## Phase 2: 训练器改造 ✅（HYPIR/trainer/base.py）

### T2.1 新增 import
- [x] 添加 `from HYPIR.model.sed_discriminator import CLIP_Semantic_extractor, SeD_P`

### T2.2 新增 init_sed_discriminator() 方法
- [x] 创建 semantic_extractor（冻结 CLIP RN50）
- [x] 创建 D_sed（SeD_P，可训练）

### T2.3 修改 init_models()
- [x] 在 init_discriminator() 后调用 init_sed_discriminator()

### T2.4 新增 _prepare_sed_inputs() 方法
- [x] 输入图像 resize 到 256×256
- [x] GT resize 到 256×256
- [x] 从 GT_256 提取语义特征

### T2.5 修改 init_optimizers()
- [x] 新增 D_sed_params + D_sed_opt

### T2.6 修改 prepare_all()
- [x] attrs 扩展为 `["G", "D", "D_sed", "G_opt", "D_opt", "D_sed_opt", "dataloader"]`

### T2.7 修改 optimize_generator()
- [x] D_sed 设为 eval + requires_grad_(False)
- [x] 新增 loss_sed = BCEWithLogitsLoss(pred, ones) × lambda_gan_sed
- [x] loss_dict 增加 G_sed 键

### T2.8 修改 optimize_discriminator()
- [x] 保留原始 ConvNeXt D 训练块不变
- [x] 新增 SeD D 训练块（BCE real/fake）
- [x] loss_dict 增加 D_sed、D_sed_logits_real、D_sed_logits_fake

### T2.9 修改 log_grads()
- [x] D_sed 设为 eval + requires_grad_(False)
- [x] losses 列表增加 ("sed", loss_sed)

---

## Phase 3: 配置 ✅（configs/sd2_train.yaml）

### T3.1 新增配置项
- [x] `lambda_gan_sed: 0.05`
- [x] `sed_type: "P"`
- [x] `sed_resize: 256`

---

## Phase 4: 验证

### T4.1 模块实例化测试 ✅
- [x] Semantic shape: (1,1024,16,16) ✅
- [x] SeD_P output shape: (1,1,30,30) ✅

### T4.2 Resize 兼容性测试 ✅
- [x] 512→256: input=(1,3,256,256), semantic=(1,1024,16,16), output=(1,1,30,30) ✅
- [x] Backward pass OK, loss=0.6913 ✅

### T4.3 语法检查 ✅
- [x] base.py syntax OK ✅
- [x] All trainer imports OK ✅
- [x] Config lambda_gan_sed=0.05, sed_type=P, sed_resize=256 ✅

### T4.4 训练冒烟测试 ⬜
- [ ] 10 步无报错
- [ ] loss_dict 含 G_sed / D_sed
- [ ] tensorboard 正常记录

### T4.5 显存监控 ⬜
- [ ] 新增 ~250MB 确认

---

## Phase 5: 消融实验 ⬜

| 实验 | 配置 | PSNR | SSIM | LPIPS | FID | 状态 |
|------|------|------|------|-------|-----|------|
| 基线 | λ_gan_sed=0 | - | - | - | - | ⬜ |
| +SeD λ=0.05 | λ_gan_sed=0.05 | - | - | - | - | ⬜ |
| +SeD λ=0.1 | λ_gan_sed=0.1 | - | - | - | - | ⬜ |
| +SeD λ=0.2 | λ_gan_sed=0.2 | - | - | - | - | ⬜ |

---

## 变更文件清单

| 文件 | 操作 | 行数变化 |
|------|------|----------|
| `HYPIR/model/module_attention.py` | 新增 | ~190 行 |
| `HYPIR/model/sed_discriminator.py` | 新增 | ~110 行 |
| `HYPIR/trainer/base.py` | 修改 | import + init_sed + _prepare_sed + optimize_G/D + log_grads |
| `configs/sd2_train.yaml` | 修改 | 末尾 +3 行 |

## 问题与决策记录

| 日期 | 问题 | 决策 | 原因 |
|------|------|------|------|
| 2026-04-24 | 分辨率不匹配 (512 vs 256) | resize 到 256 | SeD 硬编码 256，resize 改动最小 |
| 2026-04-24 | 是否替换原判别器 | 保留原 D + 新增 SeD | 保留全分辨率判别能力 |
| 2026-04-24 | SeD GAN loss 权重 | 0.05 | SeD 源码 loss_g=0.01，适配 HYPIR scale |
